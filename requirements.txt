# Core dependencies
aiogram>=3.31.0,<4.0
motor>=3.7.1,<4.0
curl_cffi>=0.16.3,<1.0
aiohttp>=3.14.3,<4.0
beautifulsoup4>=4.12.0,<5.0
pymongo>=4.18.0,<5.0
cryptography>=50.0.1,<51.0
python-dotenv>=1.0.0,<2.0
colorama>=0.4.6,<1.0
# Stocks Data
yfinance>=1.7.0,<2.0
pandas>=3.0.5,<4.0

# System Monitoring (Admin panel)
psutil>=7.2.2,<8.0

# Optional: Redis distributed cache + FSM storage
# Set settings.redis.url to enable; falls back to MemoryCache if not configured
redis>=5.0.0,<6.0

# Optional: Sentry error tracking
# Set settings.sentry.dsn to enable; no-op if dsn is empty
sentry-sdk>=2.68.1,<3.0
